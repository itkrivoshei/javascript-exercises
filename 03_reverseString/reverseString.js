const reverseString = function (text) {
  return text.split('').reverse().join('');

  // const res = [];

  // for (i = 0, j = text.length - 1; text.length - 1 >= i; i++, j--) {
  //   res[i] = text[j];
  // }

  // return res.join('');
};

// Do not edit below this line
module.exports = reverseString;
